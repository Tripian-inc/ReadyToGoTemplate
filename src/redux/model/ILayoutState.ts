interface ILayoutState {
  initLoading: boolean;
  // navbarVisible: boolean;
  tripListVisible: boolean;
  // focusInfoVisible: boolean;
  // searchVisible: boolean;
  // favoritesVisible: boolean;
  // favoritesModalVisible: boolean;
  // bookingsModalVisible: boolean;
  // localExperiencesModalVisible: boolean;
}

export default ILayoutState;
